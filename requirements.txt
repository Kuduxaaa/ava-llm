# Canonical dependency list lives in pyproject.toml.
#   pip install -e ".[all,dev]"
# This file is kept for `pip install -r requirements.txt` workflows.
torch>=2.4
numpy>=1.24
sentencepiece>=0.2
safetensors>=0.4
datasets>=2.18
huggingface_hub>=0.22
matplotlib>=3.7
